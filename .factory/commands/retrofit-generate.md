---
description: "Run Sigma ops/retrofit-generate"
allowed-tools:
  - Read
  - Write
  - Edit
  - Bash
  - WebFetch
---

# /retrofit-generate

Invoke the **retrofit-generate** agent from Sigma Protocol.

This command runs the full retrofit-generate workflow including:
- All HITL (Human-in-the-Loop) checkpoints
- MCP research integration
- Quality verification gates

**Usage:** `/retrofit-generate [your input here]`

@ops-retrofit-generate
