---
description: "Run Sigma ops/orchestrate"
allowed-tools:
  - Read
  - Write
  - Edit
  - Bash
  - WebFetch
---

# /orchestrate

Invoke the **orchestrate** agent from Sigma Protocol.

This command runs the full orchestrate workflow including:
- All HITL (Human-in-the-Loop) checkpoints
- MCP research integration
- Quality verification gates

**Usage:** `/orchestrate [your input here]`

@ops-orchestrate
