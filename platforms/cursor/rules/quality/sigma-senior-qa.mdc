---
description: "Sigma Senior QA - Test strategies, automation, coverage analysis. Use for testing and quality assurance."
globs: ["**/*.test.*", "**/*.spec.*", "**/tests/**/*", "**/e2e/**/*", "playwright.config.*"]
keywords: ["test", "qa", "coverage", "e2e", "unit", "integration", "automation", "playwright", "vitest"]
---

# Sigma Senior QA Skill

Comprehensive QA, test automation, and quality strategies.

## Testing Pyramid

```
        /\
       /  \    E2E (10%) - Critical paths only
      /────\
     /      \  Integration (20%) - Component interactions
    /────────\
   /          \ Unit (70%) - Fast, isolated, business logic
```

## Test Strategy Template

```markdown
## Test Strategy: [Feature]

### Scope
- In Scope: [What's tested]
- Out of Scope: [What's not, and why]

### Test Types
- Unit: [Yes/No] - [Scope]
- Integration: [Yes/No] - [Scope]
- E2E: [Yes/No] - [Critical paths]

### Test Cases

| ID | Component | Scenario | Expected |
|----|-----------|----------|----------|
| U1 | [Module] | [Test] | [Result] |
```

## Test Patterns

### Unit Test (AAA)

```typescript
describe('calculateDiscount', () => {
  it('should apply 10% for orders over $100', () => {
    // Arrange
    const order = { total: 150 };
    
    // Act
    const result = calculateDiscount(order);
    
    // Assert
    expect(result).toBe(15);
  });
});
```

### Integration Test

```typescript
describe('POST /api/orders', () => {
  it('should create order and update inventory', async () => {
    const response = await request(app)
      .post('/api/orders')
      .send({ items: [...] })
      .expect(201);

    // Verify side effects
    const inventory = await db.getInventory('prod-1');
    expect(inventory.quantity).toBe(8);
  });
});
```

## Coverage Targets

| Code Type | Target |
|-----------|--------|
| Critical paths (auth, payments) | 90%+ |
| Business logic | 70%+ |
| UI components | 50%+ |

## QA Checklist

- [ ] All tests pass
- [ ] Coverage targets met
- [ ] Cross-browser tested
- [ ] Mobile responsive verified
- [ ] Accessibility audit passed
