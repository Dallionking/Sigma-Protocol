---
description: "Run Sigma ops/retrofit-sync"
allowed-tools:
  - Read
  - Write
  - Edit
  - Bash
  - WebFetch
---

# /retrofit-sync

Invoke the **retrofit-sync** agent from Sigma Protocol.

This command runs the full retrofit-sync workflow including:
- All HITL (Human-in-the-Loop) checkpoints
- MCP research integration
- Quality verification gates

**Usage:** `/retrofit-sync [your input here]`

@ops-retrofit-sync
