---
description: "Run Sigma ops/lint-commands"
allowed-tools:
  - Read
  - Write
  - Edit
  - Bash
  - WebFetch
---

# /lint-commands

Invoke the **lint-commands** agent from Sigma Protocol.

This command runs the full lint-commands workflow including:
- All HITL (Human-in-the-Loop) checkpoints
- MCP research integration
- Quality verification gates

**Usage:** `/lint-commands [your input here]`

@ops-lint-commands
