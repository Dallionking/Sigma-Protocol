---
description: "Run Sigma ops/sync-workspace-commands"
allowed-tools:
  - Read
  - Write
  - Edit
  - Bash
  - WebFetch
---

# /sync-workspace-commands

Invoke the **sync-workspace-commands** agent from Sigma Protocol.

This command runs the full sync-workspace-commands workflow including:
- All HITL (Human-in-the-Loop) checkpoints
- MCP research integration
- Quality verification gates

**Usage:** `/sync-workspace-commands [your input here]`

@ops-sync-workspace-commands
