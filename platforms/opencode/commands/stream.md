---
description: "Run Sigma ops/stream"
allowed-tools:
  - Read
  - Write
  - Edit
  - Bash
  - WebFetch
---

# /stream

Invoke the **stream** agent from Sigma Protocol.

This command runs the full stream workflow including:
- All HITL (Human-in-the-Loop) checkpoints
- MCP research integration
- Quality verification gates

**Usage:** `/stream [your input here]`

@ops-stream
